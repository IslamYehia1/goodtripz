type propsType = {
    suggestions: Array<string[]>;
};
const Suggestions = (props: propsType) => {
    return (
        <ul>
            <li>Hi</li>
        </ul>
    );
};

export default Suggestions;
