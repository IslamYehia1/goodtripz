export type propsType = {
  duration: string;
  price: string;
  stopsNumber: string;
  departureTime: string;
  arrivalTime: string;
  departure: string;
  destination: string;
  stops: Array<string>;
  airports: any;
  // searchTerm: any;
};
