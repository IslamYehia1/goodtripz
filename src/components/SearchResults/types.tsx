export type FlightsSideBarT = {
    closeModal: () => void;
    isFullScreen: Boolean;
    isMobile: Boolean;
};

export type hotelsSideBarT = {
    isFullScreen: Boolean;
    isMobile: Boolean;
    closeModal: () => void;
};

export type searchResultsT = {
    className: string;
};
